# Contract: Demo Cinematic Sync Indicator

## Context (Entry)
Demo the new 3-layer guard system. We will update `SyncStatus.tsx` to have a "Thinking" animation during network activity.

## Definition of Done (Exit)
1. `SyncStatus.tsx` shows a golden glow when `isReceiving` is true.
2. Verified via Playwright.

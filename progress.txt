# Progress Log - Ralph Loop

This file tracks the progress of the Ralph loop iterations.

## [2026-04-12] - STORY-026
- **What was implemented**:
  - **STORY-026**: 居中空卡占位符文本。修改了 `PatronSlot.tsx` 中渲染空卡槽占位符的布局，将原有的 `flex-col justify-between` 和绝对定位的文本改为 `flex-col items-center justify-center gap-2`，确保了文字和三角形图标在容器内绝对居中。
- **Files changed**: `src/components/PatronSlot.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在需要将多个元素作为一个整体在容器中居中时，使用 `flex flex-col items-center justify-center` 并配合 `gap` 是最可靠的方法，避免了绝对定位带来的居中偏移问题。
---

## [2026-04-12] - STORY-025
- **What was implemented**:
  - **STORY-025**: 强化 Patron 底色质感。为 `PatronSlot.tsx` 中的贵族卡牌深红色底板增加了径向渐变（`radial-gradient`）和深色内阴影（`shadow-inner`），消除了原有的扁平“塑料感”，使其更符合暗黑奇幻风格。
- **Files changed**: `src/components/PatronSlot.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在 Tailwind 中使用任意值语法（如 `bg-[radial-gradient(...)]`）可以非常方便地实现复杂的背景效果，而无需在 CSS 文件中定义类。
  - 增加内阴影 `shadow-[inset_0_0_15px_rgba(0,0,0,0.8)]` 可以有效增加元素的体积感和深度感，消除扁平感。
---

## [2026-04-12] - STORY-024
- **What was implemented**:
  - **STORY-024**: 替换 Wildcard Emoji 为矢量图。将 `ResourceIcon.tsx` 中的 `TRUE_SOUL_TADPOLE` 从纯文本字符 '✹' 替换为精致的 SVG 灵吸怪/蝌蚪图标，使其符合暗黑奇幻风格。
- **Files changed**: `src/components/common/ResourceIcon.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在 React 中，将 `Record<string, string>` 的字典值类型改为 `Record<string, React.ReactNode>` 可以非常方便地将纯文本 Emoji 替换为复杂的 SVG 图标，而不需要修改使用该字典的渲染逻辑。
  - 使用 SVG 的 `width="1em"` 和 `height="1em"` 可以让 SVG 图标自动继承父元素的字体大小，非常适合用作文本内嵌图标。
---

## [2026-04-12] - STORY-023
- **What was implemented**:
  - **STORY-023**: 对齐设置齿轮。将设置齿轮按钮从 `GameArena.tsx` 左侧列底部移动到 `ResourceMatrix.tsx` 中，与 True Soul Tadpole 代币（资源图标）水平居中对齐，消除了孤立感并确保了视觉对齐。
- **Files changed**: `src/components/GameArena.tsx`, `src/components/ResourceMatrix.tsx`, `src/store/uiStore.ts`
- **💣 踩坑与经验 (Learnings):**
  - 当需要将一个组件与另一个组件内的子元素对齐时，最直接的方法是将其移动到该组件内，或者使用全局状态共享控制逻辑，从而实现更紧密的布局耦合。
  - 使用 `flex items-center` 和 `justify-between` 可以轻松实现左右分布且垂直居中的对齐效果。
---

## [2026-04-12] - STORY-022
- **What was implemented**:
  - **STORY-022**: 防止代币挤压变形。为 `Token.tsx` 的基础类增加了 `shrink-0`（已有 `aspect-square`），并为 `ResourceIcon.tsx` 增加了 `inline-flex items-center justify-center aspect-square shrink-0`，确保代币和图标在任何情况下都保持正圆/正方形，不被挤压。
- **Files changed**: `src/components/Token.tsx`, `src/components/common/ResourceIcon.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在 Flex 布局中，即使设置了 `aspect-square`，如果不设置 `shrink-0`，元素在空间不足时仍会被挤压变形。
  - 为 `ResourceIcon` 这种纯文本图标添加 `inline-flex` 和 `aspect-square` 可以使其表现得像一个真正的正方形图标容器。
---

## [2026-04-12] - STORY-021
- **What was implemented**:
  - **STORY-021**: 修复左下角设置齿轮孤立。将设置齿轮图标从 `PublicResourcePool.tsx` 移动到 `GameArena.tsx` 的左侧资产栏底部，使其与其他 UI 元素对齐，消除了孤立感。同时将设置 Modal 也移至 `GameArena.tsx` 中。
- **Files changed**: `src/components/PublicResourcePool.tsx`, `src/components/GameArena.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 移动组件时，需要注意状态（如 `isSettingsOpen`）和副作用（如修改 document 样式的 `handleScaleChange`）的迁移。
  - 将孤立的 UI 元素移动到逻辑相关的区域（如将设置按钮移至左侧控制/资产面板底部）可以提升 UI 的整体感和对齐度。
---

## [2026-04-12] - STORY-020
- **What was implemented**:
  - **STORY-020**: 优化历史抽屉入口。将 `GameArena.tsx` 中历史抽屉入口按钮的位置调整为 `fixed bottom-4 right-4`，并将图标替换为精致的羊皮纸卷轴图标 `📜`。
- **Files changed**: `src/components/GameArena.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 使用 Unicode 符号如 `📜` 可以快速实现奇幻风格的图标，且具有良好的矢量特性和缩放性。
  - 调整 `fixed` 定位属性（从 `bottom-6 right-6` 到 `bottom-4 right-4`）可以使元素更贴近角落，符合常见 UI 习惯。
---

## [2026-04-12] - STORY-019
- **What was implemented**:
  - **STORY-019**: 平衡网格列间距。调整了 `GameArena.tsx` 中主 Grid 的列宽为对称的 `[260px_1fr_260px]` 并将 `gap` 从 `4` 增加到 `6`，确保左侧与中间的间距、中间与右侧的间距平衡一致，消除了视觉重心偏左的问题。
- **Files changed**: `src/components/GameArena.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 当使用 CSS Grid 布局时，如果两侧列宽不一致（如 250px vs 200px），会导致中间的 `1fr` 列偏离屏幕中心，造成视觉重心偏移。
  - 将两侧列宽调整为对称的尺寸（如 260px）并统一使用 `gap`，可以有效使整体布局居中平衡。
---

## [2026-04-12] - STORY-018
- **What was implemented**:
  - **STORY-018**: 增加保留卡槽 Padding。为 `PlayerBoard.tsx` 中 `RESERVED CARDS` 下方的虚线框容器增加了 `pb-4`，确保它们不会紧贴下边缘，留出呼吸空间。
- **Files changed**: `src/components/PlayerBoard.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在 Flex 布局中，为容器增加 `pb-4` 可以有效拉开与下方内容的间距，提供呼吸感。
---

## [2026-04-12] - STORY-017
- **What was implemented**:
  - **STORY-017**: 统一卡牌成本符号与资产图标。将 `Card.tsx` 中渲染卡牌底部成本要求的地方，从使用普通的 Emoji 图标替换为使用 `ResourceIcon` 组件，从而与左侧资产栏的奇幻符号保持一致。
- **Files changed**: `src/components/Card.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在替换硬编码的图形或 Emoji 时，复用已有的业务组件（如 `ResourceIcon`）不仅能快速实现风格的统一，还能保证符号与资产列表的完全一致。
  - 注意 `ResourceIcon` 组件的尺寸属性，选择合适的尺寸（如 `sm`）以匹配原有的布局大小。
---

## [2026-04-12] - STORY-016
- **What was implemented**:
  - **STORY-016**: 优化威望盾牌锚点。将 `Avatar.tsx` 中威望盾牌（Prestige Badge）的定位从头像下方的 `bottom-[-6px] left-1/2 -translate-x-1/2` 移至左上角的 `-top-1 -left-1`，作为角标显示，消除了原先悬浮在头像下方的突兀感，使视觉更和谐。
- **Files changed**: `src/components/common/Avatar.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在处理绝对定位的角标时，将其移至角落（如 `-top-1 -left-1`）通常比悬挂在边缘居中（如 `bottom-[-6px] left-1/2`）更符合常见的 UI 规范，视觉上也更稳定。
  - 复用现有的 `rounded-full` 样式 and 渐变色，只需调整定位即可快速改善视觉平衡。
---

## [2026-04-12] - STORY-015
- **What was implemented**:
  - **STORY-015**: 修复牌库厚度色彩割裂。将 `DeckStack.tsx` 中模拟牌库厚度的侧边线条颜色从黄色（`#b58a3e`）改为羊皮纸色（`#e8e2d2`），使牌库侧边颜色与整体风格统一，消除色彩割裂感。
- **Files changed**: `src/components/DeckStack.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在处理卡牌堆叠效果时，使用阴影（`box-shadow`）来模拟厚度是一种常见手法。如果线条颜色与正面材质或边框颜色不一致，会导致色彩割裂。
  - 将黄色线条改为羊皮纸色（`#e8e2d2`）能更好地模拟羊皮纸卡牌堆叠的物理质感，提升整体的材质感。
---

## [2026-04-12] - STORY-014
- **What was implemented**:
  - **STORY-014**: 替换卡牌右上角菱形为奇幻图标。将 `Card.tsx` 中右上角代表产出的菱形 `div` 替换为使用 `ResourceIcon` 组件，并移除 `clip-path` 改为 `rounded-full` 圆形背景，使图标与左侧资产栏风格一致。
- **Files changed**: `src/components/Card.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在替换硬编码的图形（如 `clip-path` 菱形）时，复用已有的业务组件（如 `ResourceIcon`）不仅能快速实现奇幻风格的统一，还能保证符号与资产列表的完全一致。
  - 将背景形状从菱形改为圆形，能更好地承载居中的 Unicode 符号，避免尖角与符号发生视觉冲突。
---

## [2026-04-12] - STORY-013
- **What was implemented**:
  - **STORY-013**: 卡牌底部成本增加 Padding。将 `Card.tsx` 中包裹底部成本圆圈的容器的 `p-1` 改为 `pt-1 px-1 pb-2`，增加了底部的间距，确保成本圆圈的下边缘不会紧贴或切穿卡牌的外框。
- **Files changed**: `src/components/Card.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在使用 Tailwind 调整特定方向的 padding 时，如果原先使用的是 `p-x`（四周统一），改为 `pt-x px-x pb-y` 的组合可以更精准地控制特定方向的间距，同时保持其他方向一致，避免因类名覆盖顺序导致的非预期效果。
---

## [2026-04-12] - STORY-012
- **What was implemented**:
  - **STORY-012**: 居中卡背魔法阵。将 `DeckStack.tsx` 中背景的魔法阵 SVG 移出 background-image，改为独立的绝对定位 `div`，使用 `top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2` 确保其与卡背和红圈绝对居中，消除了 Y 轴偏移。
- **Files changed**: `src/components/DeckStack.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在使用 `background-position: center` 时，如果容器与背景图片的宽高比不一致，可能会因为浏览器的解析差异导致微小的 Y 轴偏移。
  - 使用绝对定位 `top-1/2 left-1/2` 配合 `translate` 是确保元素在父容器中绝对居中的最可靠方法，尤其是在需要多个层叠元素完美对齐的场景下。
---

## [2026-04-12] - STORY-011
- **What was implemented**:
  - **STORY-011**: 优化卡背“二维红漆”。将 `DeckStack.tsx` 中卡背红圈的颜色改为暗血红色（`bg-[#8B0000]`），并移除了复杂的径向渐变和阴影，改用强烈的内阴影（`shadow-inner`），以模拟封蜡的体积感。同时修复了 `ResourceMatrix.tsx` 中的 lint 错误。
- **Files changed**: `src/components/DeckStack.tsx`, `src/components/ResourceMatrix.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在处理视觉优化任务时，遵循具体指令（如使用 `shadow-inner` 和 `bg-[#8B0000]`）可以快速达到预期效果。
  - 顺手修复无关文件的 lint 错误（如未使用的变量）可以保持项目的健康状态，确保后续流程顺利进行。
---

## [2026-04-12] - STORY-010
- **What was implemented**:
  - **STORY-010**: 卡牌数字加大。将 `Card.tsx` 中卡牌底部成本圆圈里的数字字号从 `text-2xl` 加大到 `text-3xl`，使其明显变大。
- **Files changed**: `src/components/Card.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在 Tailwind 中，从 `text-2xl` 提升到 `text-3xl` 可以有效增加字号约 25%（从 24px 到 30px），满足“字号加大 20%”的要求。
---

## [2026-04-12] - STORY-009
- **What was implemented**:
  - **STORY-009**: 顶部悬浮框融入主界面。移除了 `GameArena.tsx` 中 Turns 指示器的外框样式（背景、边框、阴影），使其变成纯文本和头像的组合，不再像个独立的弹窗，更好地融入了主界面。
- **Files changed**: `src/components/GameArena.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 对于漂浮在页面顶部的独立小组件，如果其背景 and 边框使其看起来像个脱离文档流的“Debug 弹窗”，去掉外框样式使其变成纯文本/图标组合，往往能更好地融入整体 UI。
---

## [2026-04-12] - STORY-008
- **What was implemented**:
  - **STORY-008**: 顶部 TURNS 进度条增加解释。在 `PublicResourcePool.tsx` 中，为黄色的进度条增加了一个显式的 `Turn Progress` Label，解释了进度条代表的具体业务含义。
- **Files changed**: `src/components/PublicResourcePool.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在为无状态或仅有视觉呈现的组件（如 Placeholder 进度条）增加解释时，使用清晰的文字 Label（如 "Turn Progress"）可以有效提升用户的理解度，避免猜测。
---

## [2026-04-12] - STORY-007
- **What was implemented**:
  - **STORY-007**: 修复了资产列表的过度黯淡。移除了 `ResourceMatrix.tsx` 中资源项的条件透明度，统一改为常驻的 `opacity-100`，确保玩家在任何时候都能清晰看到资产数量。
- **Files changed**: `src/components/ResourceMatrix.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 为了强调“资产数量”的可见性，应避免在零值时过度降低透明度。移除不必要的条件透明度判断，直接拉满透明度（`opacity-100`），可以有效提升 UI 的可读性。
---

## [2026-04-12] - STORY-006
- **What was implemented**:
  - **STORY-006**: 修复了 CARD MARKET 标题不可见。将 `CardMarket.tsx` 中标题的颜色从 `text-parchment` 改为高亮的金色 `text-[#bf953f]`，确保在深色背景下清晰可见。
- **Files changed**: `src/components/CardMarket.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在深色背景下，`text-parchment` 可能亮度不足或未正确解析，使用明确的金色十六进制颜色（如 `#bf953f`）可以显著提升可见度和视觉效果。
---

## [2026-04-12] - STORY-005
- **What was implemented**:
  - **STORY-005**: 修复了贵族威望分数的物理碰撞。将 `PrestigeBadge.tsx` 中的定位从 `top-1 right-1` 改为 `-top-2 -right-2`，并使 `PatronSlot.tsx` 的外层容器为 `relative`，将 `PrestigeBadge` 移出带有 `overflow-hidden` 的内层容器，确保角标悬浮显示且不被裁剪。
- **Files changed**: `src/components/PrestigeBadge.tsx`, `src/components/PatronSlot.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 当子元素需要超出父容器边界显示（如负值的 absolute 定位）时，必须确保父容器没有 `overflow-hidden`。
  - 如果父容器必须使用 `overflow-hidden`，可以将需要溢出显示的子元素移到与该容器同级的位置，并使用共同的 `relative` 祖先容器进行定位。
---

## [2026-04-12] - STORY-004
- **What was implemented**:
  - **STORY-004**: 修复了贵族名字被截断的问题。将 `PatronSlot.tsx` 中卡牌的宽度从 `w-[100px]` 扩大至 `w-[120px]`，并将名字字体大小从 `text-xs` 缩小至 `text-[11px]`，同时移除了 `truncate` 类，确保名字（如 WITHERS, RAPHAEL, VLAAKITH）能够完整显示。
- **Files changed**: `src/components/PatronSlot.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在右侧边栏宽度允许的情况下，适当增加卡牌宽度是解决长文本截断的有效方法。
  - 结合微调字号和移除 `truncate` 可以确保内容在容器内完整呈现，避免不必要的省略号。
---

## [2026-04-12] - STORY-003
- **What was implemented**:
  - **STORY-003**: 修复了 Gale 名字被截断的问题。移除了 `PlayerAvatar.tsx` 中包裹玩家名字的 `span` 上的 `truncate` 类，确保名字完整显示。
- **Files changed**: `src/components/PlayerAvatar.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在 Flex 布局中，`truncate` 类会自动应用 `overflow: hidden` 和 `text-overflow: ellipsis`，如果不希望文本被截断，应避免使用该类，或者确保父容器有足够的空间。对于较短的名字如 "Gale"，移除 `truncate` 即可解决问题。
---

## [2026-04-12] - STORY-002
- **What was implemented**:
  - **STORY-002**: 修复了 Astarion 名字的“隐形”问题。在 `GameArena.tsx` 中，将当前回合玩家的名字颜色从 `text-[#c9a063]` 改为常驻的羊皮纸色 `text-[#E8E2D2]`，并通过 `font-bold` 突出显示。
- **Files changed**: `src/components/GameArena.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在深色背景下，即使是金色也可能因为亮度不足而导致阅读困难。统一使用高亮度的羊皮纸色（`#E8E2D2`）能有效提升对比度和可读性。
---

## [2026-04-12] - STORY-024 to STORY-030
- **What was implemented**:
  - **STORY-024**: 修复了可购买高亮，移除了无效的 `shadow-glow-arcane`，改为 `border-[#bf953f] shadow-[0_0_15px_rgba(212,175,55,0.6)] animate-pulse`。
  - **STORY-025**: 修复了悬停按钮，移除了无效的 `bg-dark-red` 等类，改为有效的 Tailwind 类，并为不同操作按钮（Select/Buy/Reserve）设置了不同的悬停颜色（黄/蓝/红）。
  - **STORY-026**: 强化了顶部资源池的背景，改为 `#0a0a0f`，增加 padding，并添加了深内阴影，营造出“凹槽”效果。
  - **STORY-027**: 在 `ResourceMatrix.tsx` 中，当某个资源的 Token 和 Bonus 均为 0 时，将该行的透明度降低至 `opacity-40`，减少视觉噪音。
  - **STORY-028**: 将 `EmptyCardSlot.tsx` 中背景法阵 SVG 的 opacity 从 0.60/0.70 提高到 0.95/1.0，使暗色区域的纹理更清晰。
  - **STORY-029**: 将 `GameArena.tsx` 中的 `Turns` 模块从左侧栏顶部移到了整个 Grid 的上方，并居中显示，解决了其被边缘隔离在“视觉死角”的问题。
  - **STORY-030**: 为 `GameArena.tsx` 右侧的 Patrons 容器增加了 `items-center`，将 Patron 卡牌居中，消除了右侧大片空白导致的视觉塌陷。
- **Files changed**: `src/components/Card.tsx`, `src/components/PublicResourcePool.tsx`, `src/components/ResourceMatrix.tsx`, `src/components/EmptyCardSlot.tsx`, `src/components/GameArena.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在使用自定义 CSS 变量配合 Tailwind 时，必须确保类名存在或使用任意值语法（如 `shadow-[var(--...)]`），否则样式会失效。
  - 居中对齐是解决小组件在大容器中产生“单侧空白”的最快方案，尤其是在右侧边栏中。
---

## [2026-04-12] - STORY-018 to STORY-023
- **What was implemented**:
  - **STORY-018**: 为 `GameArena.tsx` 中的左侧列容器增加了 `pl-2`，防止头像阴影和边缘被屏幕边缘物理切割。
  - **STORY-019**: 将 `PlayerBoard.tsx` 中 Reserved Cards 容器的 padding 从 `p-3` 增加到 `p-4`，为虚线槽位提供了更多呼吸空间。
  - **STORY-020**: 将 `CardMarket.tsx` 中标题的 `mb-2` 改为 `mb-0`，有效缩减了标题与第一排卡牌之间过大的垂直间距。
  - **STORY-021**: 移除了 `PlayerBoard.tsx` 中当前玩家面板的 `border-[#8a2be2]/60` 覆盖，恢复了统一的 6px `.bg3-panel` 石质边框，通过阴影区分当前玩家。
  - **STORY-022**: 将 `GameArena.tsx` 右侧 Patrons 容器的 `gap-4` 调整为 `gap-6`，增加了卡牌之间的垂直间距，使布局更舒展。
  - **STORY-023**: 修改了 `PublicResourcePool.tsx` 中 Turns 进度条底槽的样式，背景改为 `#0a0a0f`，边框改为 `border-[#bf953f]`，强化了终点启示。
- **Files changed**: `src/components/GameArena.tsx`, `src/components/PlayerBoard.tsx`, `src/components/CardMarket.tsx`, `src/components/PublicResourcePool.tsx`
- **💣 踩坑与经验 (Learnings):**
  - Tailwind 的 `border-颜色` 类如果没有指定宽度，会默认重置为 1px。在继承了自定义粗边框的类（如 `.bg3-panel`）时使用它，会导致意料之外的“边框变细”现象。
  - UI 的紧凑感与呼吸感需要精细平衡，适当增加 `gap` 或移除多余的 `margin` 可以显著改善视觉节奏。
---

## [2026-04-12] - STORY-012 to STORY-017
- **What was implemented**:
  - **STORY-012**: 将 `PublicResourcePool.tsx` 中百搭代币的容器从 `rounded-xl` 改为 `rounded-full`，统一了几何形态。
  - **STORY-013**: 强化了 `DeckStack.tsx` 中卡背红圈的阴影，增加了内高光和外投影，提升了体积感。
  - **STORY-014**: 将 `ResourceIcon.tsx` 中的 Emoji 替换为更具奇幻感的 Unicode 符号（❂, ✦, ❦, ▲, ◆, ✹）。
  - **STORY-015**: 将 `PatronSlot.tsx` 中的三角形占位符替换为“圆中嵌菱形”的奥术几何图案。
  - **STORY-016**: 将 `PlayerBoard.tsx` 中的 Assets 标题字体改为 `font-fantasy`，消除了无衬线体残留。
  - **STORY-017**: 为 `Card.tsx` 的卡牌增加了 `border-2 border-[#bf953f]/70` 金色边框，提升了金属质感。
- **Files changed**: `src/components/PublicResourcePool.tsx`, `src/components/DeckStack.tsx`, `src/components/common/ResourceIcon.tsx`, `src/components/PatronSlot.tsx`, `src/components/PlayerBoard.tsx`, `src/components/Card.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在没有安装图标库的情况下，精心挑选 the Unicode 符号可以作为 Emoji 的优秀替代品，既保留了矢量特性，又符合暗黑奇幻风格。
  - 给卡牌增加适度的金色半透明边框（如 `border-[#bf953f]/70`），可以在不破坏原有设计的前提下，显著提升 UI 的精致感。
---

## [2026-04-12] - STORY-006 to STORY-011
- **What was implemented**: 
  - **STORY-006**: 移除了 `Token.tsx` 中 `countClasses` 的 `leading-none`，解决了代币数字 Y 轴偏移、无法垂直居中的问题。
  - **STORY-007**: 调整了 `DeckStack.tsx` 中卡背数字 and 罗马数字的字号（3xl->2xl, base->lg），优化了比例。
  - **STORY-008**: 移除了 `ResourceMatrix.tsx` 中蝌蚪资源容器的 `justify-center`，使其与上方常规资源左对齐。
  - **STORY-009**: 将 `PublicResourcePool.tsx` 中的 Turns 文本从 `text-xs` 放大为 `text-sm font-bold`。
  - **STORY-010**: 为 `PatronSlot.tsx` 中的贵族名字容器增加了 `pt-1`，解决了紧贴顶部边缘的问题。
  - **STORY-011**: 将设置齿轮从最右侧移到了中间代币区旁边，减少了孤立感。
- **Files changed**: `src/components/Token.tsx`, `src/components/DeckStack.tsx`, `src/components/ResourceMatrix.tsx`, `src/components/PublicResourcePool.tsx`, `src/components/PatronSlot.tsx`
- **💣 踩坑与经验 (Learnings):**
  - Flex 居中时，`leading-none` 可能会因为字体的 internal padding 导致垂直居中失效，移除它让浏览器按默认 line-height 处理往往效果更好。
  - 保持 UI 元素的紧凑性和物理联系（如将设置按钮移近核心操作区）能有效提升视觉整合度。
---

## [2026-04-12] - STORY-004 & STORY-005
- **What was implemented**: 
  - 修复了贵族成本物理出格，为 `PatronSlot` 内层容器增加 `overflow-hidden`，并为成本容器增加 `pb-3`。
  - 修复了贵族威望数字重叠碰撞，将 `PrestigeBadge` 的定位 from `-top-[2px] -right-[2px]` 改为 `top-1 right-1`。
- **Files changed**: `src/components/PatronSlot.tsx`, `src/components/PrestigeBadge.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 旋转的元素（如 `rotate-45`）在布局中可能会超出其原本占据的盒模型空间，需要额外注意 padding 或 overflow 设置。
  - 负值的 absolute 定位容易导致与父容器的圆角或边框发生非预期的重叠。
---

## [2026-04-12] - STORY-003
- **What was implemented**: 修复了 Gale 名字的强行截断，通过为 `PlayerBoard` 容器增加 `flex-shrink-0` 和 `min-w-0`，防止其在 flex 布局中被挤压。
- **Files changed**: `src/components/PlayerBoard.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 在复杂的 Flex 布局中，即使子元素设置了 `min-w-0`，如果父元素被挤压且没有 `flex-shrink-0`，依然可能导致内部文本截断。
---

## [2026-04-12] - STORY-002
- **What was implemented**: 修复了卡牌成本区的视觉黑洞，通过为底部栏增加 `z-10` 并修正子元素的 z-index，确保成本数字 and 图标在最上层显示。
- **Files changed**: `src/components/Card.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 之前的 `z-above` 和 `z-base` 是无效的类名（未在 CSS 中定义为类），改为标准的 Tailwind 类或行内样式，或者使用相对定位配合 z-index 变量。
---

## [2026-04-12] - STORY-001
- **What was implemented**: 修复了 Astarion 名字在深色背景下的可见性，改为羊皮纸色 (#E8E2D2)。
- **Files changed**: `src/components/GameArena.tsx`, `src/components/PlayerAvatar.tsx`
- **💣 踩坑与经验 (Learnings):**
  - 之前使用的 `var(--color-text-primary)` 在某些上下文中可能解析失败或对比度不足，直接使用具体颜色值更可靠。
---

## [2026-04-12] - Initialized tasks.json with 30 stories.
2026-04-12 19:00:46 - STORY-031: 更新 src/ai README.md - passes: true
2026-04-12 19:01:17 - STORY-032: 更新 src/assets README.md - passes: true
2026-04-12 19:02:00 - STORY-033: 更新 src/components README.md - passes: true
2026-04-12 19:02:35 - STORY-034: 更新 src/data README.md - passes: true
2026-04-12 19:03:13 - STORY-035: 更新 src/domain README.md - passes: true
2026-04-12 19:03:50 - STORY-036: 更新 src/hooks README.md - passes: true
2026-04-12 19:04:28 - STORY-037: 更新 src/network README.md - passes: true
2026-04-12 19:05:07 - STORY-038: 更新 src/repositories README.md - passes: true
2026-04-12 19:05:46 - STORY-039: 更新 src/store README.md - passes: true
2026-04-12 19:06:27 - STORY-040: 更新 src/test-utils README.md - passes: true
